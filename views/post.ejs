<!DOCTYPE html>
<html>
	<head>

		<title>Discussion Board</title>
		<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
		<link rel="stylesheet" href="css/main.css">

	</head>

	<body ng-app="myApp" ng-controller="CommentsController">
		<div class="jumbotron">
			<h1><%=post.post_title%></h1>
			by the <%=post.dept_name%> department

   	 		<div>
   	 			<p><%=post.post_date%> </p>
   	 			<p><%=post.post_content %></p>
   	 			
   	 		</div>
   	 	</div>


   	 	<section>
            <h2>Add a Comment</h2>

            <form id="comment-form">
                <div class="form-group">
                    <label for="name">Name:</label>
                    <input type="text" class="form-control" id="title" name="name"
                    ng-model="newComments.name" placeholder="Please your name (required)"/>
                </div>

                <div class="form-group">
                    <label for="title">Title:</label>
                    <input type="text" class="form-control" id="title" name="title"
                    ng-model="newComments.title" placeholder="Please Input the Title (required)"/>
                </div>

                <div class="form-group">
                    <label for="user-comment">Comment:</label>
                    <textarea class="form-control" id="user-comment" rows="4" name="comment" 
                    placeholder="Please Input Your Comments Here" ng-model="newComments.comment"></textarea>
                </div>

                <p id="rating-message" class="error-message"></p>

                <div class="form-group">
                    <button type="button" class="btn btn-success btn-lg" ng-disabled="inserting" ng-click="addComments()">
                        Submit
                    </button>
                </div>
            </form>
        </section>

        <section class="comment-section">
            <h2>Comments</h2>
            <div class="no-comments-yet" ng-if="isEmpty">
                    <p>There are no comments yet! Be the first to review this!</p>
            </div>
            <div ng-repeat="comment in comments">
                <p id="comment-title">{{comment.reply_title}}</p>
                <div class="info-comment">
                    <img src="img/no-face.jpg" alt="no face icon" width="97px" height="80px" align="left"/>
                    <p id="user-name">{{comment.reply_by}}</p>
                    <p id="comment-comment">{{comment.reply_content}}</p>
                    <p id="comment-comment">{{comment.reply_date}}</p><br>
                </div>
                <hr width="90%">                   
            </div>
        </section>


	</body>
    <script src="http://ajax.googleapis.com/ajax/libs/angularjs/1.3.14/angular.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/angular.js/1.3.15/angular-route.min.js"></script>
    <script src="https://code.jquery.com/jquery-2.1.1.min.js" type="text/javascript"></script>
    <script src="js/board.js"></script>
</html>